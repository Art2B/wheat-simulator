{
  "name": "parcel-vanilla-starter",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.html",
  "scripts": {
    "prestart": "rm -rf .parcel-cache dist",
    "start": "parcel",
    "build": "parcel build  --public-url https://art2b.github.io/wheat-simulator/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.15.4",
    "gh-pages": "^6.3.0",
    "parcel": "^2.14.0"
  }
}
